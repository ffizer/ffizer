variables:
  - name: project_name
    default_value: "{{ file_name ffizer_dst_folder }}"

scripts:
  - cmd: |
      {{#if (eq (env "OS") "windows") }}
      echo Hello World > file2.txt
      {{else}}
      echo -e "Hello World\n" > file2.txt
      {{/if}}
